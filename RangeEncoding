#include<bits/stdc++.h>
using namespace std;
class RangeEncoding
{
    public:
     int minRanges(vector <int> arr)
         {
         int range=0;
    for (int i=0;i<arr.size();i++)
        {
        if (arr[i+1]-arr[i]!=1)
        	range++;
        }
         return range;
        }
    };
